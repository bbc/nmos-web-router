/**
 * The windowing component needs to know the height of every
 * row without rendering it first. It uses this value to
 * calculate which rows should be mounted/rendered.
 *
 * This is the height of `routable-component.jsx`
 */
export const ROUTABLE_ROW_HEIGHT = 46
